# Stats Server Configuration

# Server settings
PORT=:8084
SHUTDOWN_TIMEOUT=15s

# Redis settings (required for stream consumption)
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# Consumer settings (optional - defaults provided in code)
# STREAM_KEY=stats:events
# CONSUMER_GROUP=stats-service
# CONSUMER_ID_PREFIX=stats-consumer
# BATCH_SIZE=10
# BLOCK_TIME=5s
# MAX_RETRIES=5
# RETRY_DELAY=2s
# MIN_IDLE=10s

# Database settings (if storing metrics in database)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_NAME=stats_db

# Log server settings (if using centralized logging)
# LOG_SERVER_ADDR=localhost:8082
